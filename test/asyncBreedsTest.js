import { assert } from 'chai';
import breedDetailsFromFile from '../asyncBreeds.js';

describe('#breedDetailsFromFile', () => {
  it('provides, via callback, breed details for the Bombay breed', () => {
    console.log(breedDetailsFromFile);
  });
});